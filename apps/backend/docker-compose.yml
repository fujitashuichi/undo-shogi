services:
  db:
    image: postgres:18.3
    container_name: undo-shogi-postgres
    env_file:
      - .env
    ports:
      - 5432:5432
    volumes:
      - pg-data:/var/lib/postgresql

volumes:
  pg-data: